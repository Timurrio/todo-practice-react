import { AuthModal } from '../../components/AuthModal/AuthModal';

const LoginPage: React.FC = () => {
  return (
    <>
      <AuthModal authMode="login" />
    </>
  );
};

export default LoginPage;
